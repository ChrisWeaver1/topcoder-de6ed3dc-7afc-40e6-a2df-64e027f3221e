pub mod google {
    // use crate::Translator;

    // pub struct Google {
    //     pub api_key: String,
    // }

    // impl Translator for DeepL {
    
    //     fn translate(language: String, text: String) -> Result<String, ()> {
    //         return Ok(String::from(format!("{}: {}", language, text)));
    //     }
    // }
}
